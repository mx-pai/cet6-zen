# CET-6 Zen 部署指南

本文档说明如何将试卷 PDF 托管到云端，并将整个应用发布为网站。

## 方案概述

推荐使用 **GitHub Pages + Vercel** 方案，完全免费且易于维护。

---

## 方案一：使用 Vercel 部署（推荐）

### 优点

- ✅ 完全免费
- ✅ 自动 CI/CD（推送代码即自动部署）
- ✅ 全球 CDN 加速
- ✅ 支持自定义域名
- ✅ HTTPS 自动配置

### 部署步骤

#### 1. 准备试卷文件

在项目根目录创建试卷存储目录：

```bash
mkdir -p public/papers
```

将您的试卷 PDF 放入 `public/papers/` 目录：

```
public/papers/
  ├── 2023-06-set1.pdf
  ├── 2023-06-set2.pdf
  ├── 2023-12-set1.pdf
  └── ...
```

#### 2. 创建试卷索引文件

创建 `public/papers/index.json`，列出所有试卷：

```json
{
  "papers": [
    {
      "id": "2023-06-set1",
      "title": "2023年6月 六级真题 第一套",
      "year": 2023,
      "month": 6,
      "set": 1,
      "filename": "2023-06-set1.pdf"
    },
    {
      "id": "2023-06-set2",
      "title": "2023年6月 六级真题 第二套",
      "year": 2023,
      "month": 6,
      "set": 2,
      "filename": "2023-06-set2.pdf"
    },
    {
      "id": "2023-12-set1",
      "title": "2023年12月 六级真题 第一套",
      "year": 2023,
      "month": 12,
      "set": 1,
      "filename": "2023-12-set1.pdf"
    }
  ]
}
```

#### 3. 推送到 GitHub

```bash
git add public/papers/
git commit -m "Add online paper repository"
git push origin main
```

#### 4. 在 Vercel 上部署

1. 访问 [vercel.com](https://vercel.com)
2. 使用 GitHub 账号登录
3. 点击 "New Project"
4. 选择您的 `cet6-zen` 仓库
5. 配置如下：
   - **Framework Preset**: Vite
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`
6. 点击 "Deploy"

等待 1-2 分钟，您的应用就部署完成了！

#### 5. 访问您的网站

部署完成后，Vercel 会提供一个域名，例如：

```
https://cet6-zen.vercel.app
```

---

## 方案二：使用 GitHub Pages 部署

### 优点

- ✅ 完全免费
- ✅ 与 GitHub 仓库集成
- ✅ 简单易用

### 缺点

- ⚠️ 单个文件不能超过 100MB
- ⚠️ 仓库总大小建议不超过 1GB

### 部署步骤

#### 1. 准备试卷文件（同方案一）

#### 2. 安装 gh-pages 工具

```bash
npm install --save-dev gh-pages
```

#### 3. 修改 package.json

添加部署脚本：

```json
{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && gh-pages -d dist"
  }
}
```

#### 4. 配置 vite.config.js

添加 base 路径：

```javascript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  base: '/cet6-zen/'  // 替换为您的仓库名
})
```

#### 5. 执行部署

```bash
npm run deploy
```

#### 6. 启用 GitHub Pages

1. 打开 GitHub 仓库设置
2. 进入 "Pages" 选项
3. Source 选择 `gh-pages` 分支
4. 点击保存

访问：`https://你的用户名.github.io/cet6-zen/`

---

## 方案三：使用云存储服务（适合大量试卷）

如果试卷数量很多（超过 50 套），建议使用云存储。

### 使用腾讯云 COS

#### 1. 创建 COS 存储桶

1. 登录 [腾讯云控制台](https://console.cloud.tencent.com/cos5)
2. 新建存储桶，选择"公有读私有写"
3. 上传 PDF 文件

#### 2. 获取文件 URL

上传后，每个文件都有一个 CDN URL：

```
https://your-bucket.cos.ap-guangzhou.myqcloud.com/papers/2023-06-set1.pdf
```

#### 3. 修改 index.json

使用完整 URL：

```json
{
  "papers": [
    {
      "id": "2023-06-set1",
      "title": "2023年6月 六级真题 第一套",
      "year": 2023,
      "url": "https://your-bucket.cos.ap-guangzhou.myqcloud.com/papers/2023-06-set1.pdf"
    }
  ]
}
```

### 使用阿里云 OSS

流程类似，参考 [阿里云 OSS 文档](https://help.aliyun.com/product/31815.html)。

---

## 添加在线试卷加载功能（可选）

如果您想在应用中添加"加载在线试卷"功能，可以按以下步骤：

### 1. 在 Dashboard 添加按钮

在"批量导入真题"旁边添加"加载在线试卷"按钮。

### 2. 获取在线试卷列表

```javascript
const loadOnlinePapers = async () => {
  const response = await fetch('/papers/index.json');
  const data = await response.json();
  return data.papers;
};
```

### 3. 下载并导入

用户选择试卷后，使用 fetch 下载 PDF，然后像本地文件一样导入。

---

## 注意事项

### 文件大小限制

- **GitHub**: 单个文件 < 100MB，仓库总大小 < 1GB
- **Vercel**: 单个文件 < 100MB（免费版）
- **云存储**: 无限制（按存储容量收费）

### 版权问题

⚠️ **重要**：请确保您有权分发这些试卷 PDF。公开发布真题可能涉及版权问题，建议：

- 仅用于个人学习
- 或使用官方授权的公开试卷
- 或仅在私有仓库中使用

### 建议

1. **PDF 优化**：使用 PDF 压缩工具减小文件大小
2. **文件命名**：使用统一的命名规范，如 `YYYY-MM-setN.pdf`
3. **备份**：定期备份您的试卷文件
4. **访问控制**：如需限制访问，可使用 Vercel 的 Password Protection 功能

---

## 自定义域名（可选）

### Vercel 自定义域名

1. 在 Vercel 项目设置中选择 "Domains"
2. 添加您的域名（例如：cet6.yourdomain.com）
3. 按照提示配置 DNS 记录
4. 等待 DNS 生效（通常几分钟到几小时）

### 费用

- 所有推荐的方案都是**完全免费**的（云存储除外）
- 自定义域名需要单独购买（约 ¥50-100/年）

---

## 故障排查

### 部署后 PDF 无法加载

检查：

1. PDF 文件是否在 `public/` 目录下
2. 文件路径是否正确
3. 浏览器控制台是否有 CORS 错误

### 页面显示 404

- **Vercel**: 检查 build 是否成功
- **GitHub Pages**: 确认 `base` 配置是否正确

### 文件太大无法上传

使用 Git LFS 或云存储方案。

---

## 总结

**推荐方案**：Vercel + 公开 GitHub 仓库

- 适合中小量试卷（< 50 套，< 500MB）
- 完全免费，部署简单
- 自动 CI/CD

**高级方案**：Vercel + 云存储

- 适合大量试卷
- 可控制访问权限
- 需要少量云服务费用

祝您部署顺利！🚀
